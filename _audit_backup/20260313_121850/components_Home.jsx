const Home = ({ notices, announcements, pdfReports, sliderSlides, showLogin, setShowLogin, showAdmin, setShowAdmin, events, gallery }) => {
  return (
    <div style={{ fontFamily: "'Segoe UI',sans-serif", background: '#f8f9fa', minHeight: '100vh' }}>
    </div>
  )
}

export default Home;